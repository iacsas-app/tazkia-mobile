import { StyleSheet } from 'react-native';

export const RuleStyles = StyleSheet.create({
  stack: {
    alignItems: 'flex-start',
    textAlign: 'justify',
    paddingVertical: 4,
    marginHorizontal: 24,
  },
  pressable: {
    paddingHorizontal: 8,
    marginBottom: 1,
  },
  text: {
    marginTop: 2,
    fontWeight: 'bold',
  },
});
