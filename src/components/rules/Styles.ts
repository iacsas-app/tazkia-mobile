import { StyleSheet } from 'react-native';

export const RuleStyles = StyleSheet.create({
  container: {
    alignItems: 'flex-start',
    textAlign: 'justify',
    padding: 6,
  },
  text: {
    marginTop: 2,
    fontWeight: 'bold',
  },
});
