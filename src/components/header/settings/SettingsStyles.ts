import { StyleSheet } from 'react-native';

export const SettingsStyles = StyleSheet.create({
  container: { width: 276, marginLeft: -22 },
  surface: { paddingVertical: 10, paddingHorizontal: 5 },
});
